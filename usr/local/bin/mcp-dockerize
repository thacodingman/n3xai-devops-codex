#!/bin/bash

NAME=$1

BASE=/root/mcp/servers/$NAME

cat <<EOF > $BASE/Dockerfile
FROM python:3.11
WORKDIR /app
COPY server.py .
CMD ["python","server.py"]
EOF

docker build -t mcp-$NAME $BASE

echo "Docker image created: mcp-$NAME"